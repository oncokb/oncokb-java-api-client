# OncoKB Java API clients
This repo stores auto-generated Java clients. OncoKB private client is not supposed to used in production environment. We will update the clients regularly as long as the API is out of sync. But we only make release when the public API is changed.

## Version
We version the repo using the OncoKB Public API version.
